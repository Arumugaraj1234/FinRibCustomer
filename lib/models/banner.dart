class BannerModel {
  int id;
  String imageUrl;
  int categoryId;
  int subCategoryId;
  int productId;

  BannerModel(this.id, this.imageUrl, this.categoryId, this.subCategoryId,
      this.productId);
}
